    password: "",
  const [passwordError, setPasswordError] = useState("");
<div className="flex items-center gap-2">
              <label className="text-sm font-medium text-gray-700">
                Password*
              </label>

              <div className="relative inline-block group">
                <BsFillInfoCircleFill
                  className="text-gray-400 hover:text-[#333333] transition duration-200 cursor-pointer"
                  size={16}
                />

                <div className="absolute left-6 top-1/2 -translate-y-1/2 w-64 p-3 text-xs text-white bg-black rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                  Password must contain:
                  <ul className="list-disc ml-4 mt-1 space-y-1">
                    <li>At least 8 characters</li>
                    <li>One uppercase letter</li>
                    <li>One lowercase letter</li>
                    <li>One number</li>
                    <li>One special character</li>
                  </ul>
                </div>
              </div>


            </div>


            <div className="relative w-full">
              <input
                type={showPassword ? "text" : "password"}
                name="password"
                placeholder="Password"
                value={formData.password}
                onChange={handleChange}
                className="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500"
              />
              <button
                type="button"
                onClick={() => setShowPassword(!showPassword)}
                className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-500 hover:text-[#302f2f] transition duration-200"
              >
                {showPassword ? <FiEyeOff size={20} /> : <FiEye size={20} />}
              </button>
            </div>
            {
              passwordError && (
                <p className="text-red-500 text-sm -mt-2">
                  {passwordError}
                </p>
              )
            }